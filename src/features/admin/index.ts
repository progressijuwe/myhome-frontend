/**
 * Public surface of the admin feature. Import from here, never from the
 * internals — that barrier is what keeps the feature deletable.
 */
export { PendingAccounts } from './components/PendingAccounts';
export { PendingProperties } from './components/PendingProperties';
export { ReviewActions } from './components/ReviewActions';
